import styled from "@emotion/styled";
import { COLORS } from "@/constants";

export const LoaderStyled = styled.div`
    color: ${COLORS.white};
    padding: 20px;
    text-align: center;
    font-size: 20px;
`